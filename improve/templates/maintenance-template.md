# {项目名} 维护文档

> 用途：告诉未来的你 / 模型——这个仓库怎么跑、怎么改、怎么发布。**用户不看代码地图，看的人是你（模型）**。发布 / 改代码时同步更新本文件。

## 项目概览与目标

{做什么、给谁用，一句话描述。}**边界**：{不做的事 —— 明确不提供的功能/范围，防止 scope 蔓延与误改}。

## 维护状态

个人维护项目，按需更新，PR 欢迎但不保证及时。{单文件/单程序需注明"改动前先看代码地图定位"}。

## 构建 / 运行 / 测试

- **运行**：{怎么启动：双击 HTML / 装 skill / pip install / npm start / 打包 exe，须与哪些文件同目录}
- **测试**：{自动化测试是否存在；无则写手动实测的主要路径}
- **构建**：{打包命令、产物放哪、产物是否入库/入 Release}

## 目录结构 / 代码地图

| 路径 | 职责 |
|------|------|
| `{主文件}` | {核心入口、单文件应用边界} |
| `{模块目录}` | {职责} |
| ... | ... |

{对单文件/大文件项目：补"版本号出现 N 处"锚点 + 行号，改版本必须 grep 全改}

## 发布流程（对齐 improve skill）

1. 改版本号：{改哪里，版本文件如 package.json / pyproject.toml / VERSION }——**改版本必须 `grep vX.Y.Z` 全改**，改完复查零残留
2. README「变更日志」节顶部加新版本段（分类词：新增/修复/删除/优化/版本），中英 README 同步
3. 全局 grep 旧版本零残留；确认发布资产无本机个性化信息（用户名/绝对路径/代理端口/token，只留 `~` / `%USERPROFILE%` 通用形式）
4. commit（作者 `226671264+BFRKQSB7@users.noreply.github.com`，Conventional Commits 5 前缀）→ tag `v{版本号}` → push → `gh release create`
5. 若打包产物要更新（zip/asset/版本号重打），重打包后提交一并发布

## 提交规范

Conventional Commits 轻量 5 前缀：`feat` / `fix` / `docs` / `refactor` / `chore`。小更新 + 距上次不久可覆盖发布（重打同 tag）。

## 版本纪律

semver：破坏性→x+1、新功能→y+1、修 bug→z+1。**tag 必须与版本文件/README 版本号同步**（历史教训：balance-hud v2.2.2、llm-sight VERSION 曾滞后）。历史 tag：{列表}。

## 本仓库特有坑 / 注意

- {从 git 历史 / 排障教训里提炼的本仓库独有坑}
- {数据一致性 / 双份同步 / 构建产物依赖等}